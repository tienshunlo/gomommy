<% content_for :index_headbar_filter do %>
	<!-- 總留言區 多重篩選器 filterrefic -->
	<%= render 'posts/filter_index' %>
<% end %>

<div class="section">
	<!-- 總留言區 -->
	<%= render(partial: 'posts/list',locals: { posts: @posts }) %>
</div>    
