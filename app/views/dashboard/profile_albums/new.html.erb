<div class="wrapper__box--large MaxWidth740">
	<header class="header_cate">
  		<span class="header_cate_title">
  			新增個人照片
  		</span>
	</header>
    <div class="wrapper-doctor-index">
		<% @albums.each do |album| %>
			<div class="wrapper__box--small align-center">
		    	<%= link_to image_tag(album.album_img.url(:medium), class:"poster-avatar-img  size7x7"), dashboard_profile_profile_albums_path(profile_id: current_user.id, album_id: album.id), method: :post %>
			</div>
		<% end %>
	</div>
</div>