<div class="wrapper__box--large flex-column MaxWidth740">
	<header class="header_cate">
  		<span class="header_cate_title">
  			新增醫生
  		</span>
	</header>
	<div class="discussion-form">
		<%= form_for @doctor, url: dashboard_mamabook_doctors_path, html: { multipart: true } do |doctor| %>
			<%= render partial: 'form',  locals: { f: doctor } %>
		<% end %>
	</div>
 </div>
