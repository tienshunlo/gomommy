<% if @comment.errors.any? %>
    alert("ERROR(S): <%= j @comment.errors.full_messages.join('; ') %>")
<% else %>
    $('.add_comment').prepend('<%= j render partial: 'comments/comment', locals: {comment: @comment } %>');
    $('#comment_content').val('');
    alert("Comment created");
<% end %>